import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "img.freepik.com",
      "lh3.googleusercontent.com",
      "twitter-dev-storage.s3.ap-south-1.amazonaws.com",
    ],
  },
};

export default nextConfig;
